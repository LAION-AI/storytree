Look, here is a white paper for a script writing system. I want to run it with API as a first test. And I want you to look at and implement it in a way so that I can run it with the Hyperlab API using Grok 4.6, but that I could also use it with, like, an agent that you spawn here directly that will not use directly an API call, but just do, like, write it on its own, but according to the same rules and mechanics that the script would do. So I can use it over my subscription credits here from this current Claude Code session. So that you can just spawn an Opus 5 agent and let it write it. I want you to check first all the structure. So the order should be first something that I would call a story root, where we have the definitions of the genre, the target audience, the style, like a few sentences about the story, etc., and other things to keep in mind. Like, basically, look at the white paper that's displayed there. And from there to the exposé, to then from there to the other steps. And until we have basically the scene list, where we have for each scene the entities that are, like, in it, their starting state, the state changes, and then we have basically within each scene several beats. And in each beat is basically, like, some kind of interaction where at the end, like, there happens a state change, either mentally, emotionally, socially, physically, whatever. And entities can be persons or other, like, creatures, like characters. It could be a robot or whatever that is not a person, but it's basically the same. It can be a location, an important object, so you don't need to keep track of every object there in the story, but everything that is noteworthy and influences plots to some degree, factions, groups, whatever you think makes sense to consider as its own entity. And for each scene, we need to have, like, the input state and the changes they go through during that scene and the output, like the state at the output. And instead of writing everything down in super detailed way, it would be nice to apply some JSON patching system, so that you start in, like, in the character profiles with a dictionary-like writing style for each character and each entity, with all the detailed attributes, everything, right? Everything they could have, and is whatever relevant, and background story, and blah, blah, blah, blah, blah, blah. So that it can be patched later. And so in the background story, if it's a freeform story, there should be each sentence in this background story should be addressable with some, like, JSON key-value patch, so that later things can be patched and updated, even though the background story doesn't need to be updated usually. But so that we can go later and mechanically, like procedurally, regenerate from the original character profile or entity profile, the entity profile at the current time step T, whatever, like 25 or whatever, by just applying all the patches on the scene level or on the plot level or on the event level, so that the timeline at the event level, at the scene level, at the plot level, or whatever, has the same starting structure that is defined in the character profiles. Actually, like, so it begins with the story root definition, then the exposé, then the plot definitions. I mean, there we don't need to have this patching logic, or we could, like, but yeah, I don't know. And then we define the, later, after defining the, basically, plot contents, we define the character profiles. And there we need to write each character profile in this nested dictionary system that is patchable, that is addressable and updateable. And then from there on, we need to keep track at the event level and at the scene level, we need to keep track of all changes in a JSON patch manner that is basically like GitHub compatible. Okay? So that we later can reconstruct everything. I want you to look at this and implement this with a short story that, like, is not super complicated. Maybe just some random standard fantasy scenario, where for, like, testing purposes, you only have two plots. Yeah? And each event and each scene has to be, like, assigned or, like, pointing to at least one plot. So there can be a scene that, like, is part of two plots, or an event that is part of two plots, but each scene and each event needs to have exactly one parent plot. And each event needs to have, like, each scene needs to have an event that is a parent. Exactly one event. And at the leaves of our directed graph that we have will be the actually prose that you write based on the scene objects.