"""narrativeforge — structured narrative generation.

The inverse of the whitepaper's analysis pipeline: instead of decomposing an
existing work into layers, it composes a new one down the same layers, and
keeps every state change as an RFC 6902 patch so the world can be
reconstructed at any point in story time.
"""

__version__ = "0.1.0"
